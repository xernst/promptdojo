number: 14
slug: structured-output
title: "structured output — making the model return json"
blurb: "free-form text breaks every pipeline. learn the schema-first pattern ai uses to get reliable json back, validate it with pydantic, and catch the model's lies before they hit prod."
lessons:
  - 01-getting-json-back
  - 03-schemas-eat-prompts
# 02-validation-with-pydantic is on disk but needs `pydantic` installed
# in the validator's host Python. Pyodide can load it via micropip in
# the browser, but `pnpm validate:solutions` runs CPython directly.
# Re-register once the validator gains per-step skip or pydantic
# ships in the CI image.
